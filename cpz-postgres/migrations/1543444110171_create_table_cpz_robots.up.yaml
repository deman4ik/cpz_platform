- args:
    sql: CREATE TABLE "cpz"."robots"("id" uuid NOT NULL, "code" text NOT NULL, "name"
      text NOT NULL, "desc" text , "exchange" text NOT NULL, "asset" text NOT
      NULL, "currency" text NOT NULL, "strategyId" uuid NOT NULL,
      "timeframe" integer NOT NULL, "available_internal" boolean NOT NULL,
      "available_public" boolean NOT NULL, "adviserSettings" jsonb , "traderSettings"
      jsonb , PRIMARY KEY ("id") ); ALTER TABLE "cpz"."robots" ADD FOREIGN KEY ("exchange") REFERENCES "cpz"."exchanges"
      ("code");  ALTER TABLE "cpz"."robots" ADD FOREIGN KEY ("asset") REFERENCES "cpz"."assets"
      ("code"); ALTER TABLE "cpz"."robots" ADD FOREIGN KEY ("currency") REFERENCES "cpz"."currencies"
      ("code"); ALTER TABLE "cpz"."robots" ADD FOREIGN KEY ("strategyId") REFERENCES "cpz"."strategies"
      ("id");
  type: run_sql
- args:
    name: robots
    schema: cpz
  type: add_existing_table_or_view
