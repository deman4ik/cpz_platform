- args:
   sql: CREATE TABLE "cpz"."candles240"("id" uuid NOT NULL UNIQUE, "time" bigint NOT
      NULL, "timestamp" timestamp NOT NULL, "open" numeric NOT NULL, "high" numeric NOT
      NULL, "low" numeric NOT NULL, "close" numeric NOT NULL, "volume" numeric NOT
      NULL, "exchange" text NOT NULL, "asset" text NOT NULL, "currency" text NOT NULL,
      "type" text NOT NULL, PRIMARY KEY ("id") ); COMMENT ON TABLE "cpz"."candles240"
      IS 'Candles in 4 hours timeframe'; ALTER TABLE "cpz"."candles240" ADD FOREIGN KEY ("asset") REFERENCES "cpz"."assets"
      ("code"); ALTER TABLE "cpz"."candles240" ADD FOREIGN KEY ("currency") REFERENCES "cpz"."currencies"
      ("code"); ALTER TABLE "cpz"."candles240" ADD FOREIGN KEY ("exchange") REFERENCES "cpz"."exchanges"
      ("code");
  type: run_sql
- args:
    name: candles240
    schema: cpz
  type: add_existing_table_or_view
