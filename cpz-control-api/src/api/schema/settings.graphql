input CandlebatcherSettings {
    debug: Boolean
    proxy: String
    requiredHistoryMaxBars: Int
}

input AdviserSettings {
    debug: Boolean
    strategyParameters: JSON
    requiredHistoryCache: Boolean
    requiredHistoryMaxBars: Int
}

input TraderSettings {
    mode: String
    debug: Boolean
    slippageStep: Float
    deviation: Float
    volume: Float
}

input BacktesterSettings {
    debug: Boolean
}