input ImporterParams {
  exchange: String!
  asset: String!
  currency: String!
  timeframes: [Int!]
  mode: ImporterMode!
  settings: ImporterSettings
}

enum ImporterMode {
  importCandles
  warmupCache
}
